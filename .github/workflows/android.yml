name: Zbuduj aplikację Android (APK)

# ══════════════════════════════════════════════════════════════════════════════
# WERSJA v112 — aplikacja aktualizuje się sama + stały podpis + wydania
#
# 1. APK ŁADUJE STRONĘ Z SERWERA (server.url)
#    Dotąd index.html był zaszyty w APK, więc każda zmiana frontendu wymagała
#    nowego builda i ręcznej instalacji. Teraz aplikacja otwiera
#    https://muzyka.komodaoskara.pl — update.sh na serwerze = od razu nowa
#    wersja w telefonie. Bridge Capacitora (MediaSession, Android Auto) działa
#    dalej: Capacitor wstrzykuje go do HTML-a pobieranego z serwera.
#    Nowy APK potrzebny jest już tylko przy zmianach NATYWNYCH (native/*.java,
#    pluginy) — i o tym aplikacja sama powie (sprawdza wydania na GitHubie).
#
#    Zmiana config robiona jest TYLKO tutaj, w trakcie builda. Plik
#    capacitor.config.json w repo zostaje bez zmian, więc build iOS działa
#    dokładnie jak wcześniej.
#
# 2. SERVICE WORKER PRZEZ BRIDGE
#    Zapytania service workera omijają zwykłe przechwytywanie WebView — strona
#    podana przez SW nie miałaby wstrzykniętego bridge'a (brak MediaSession).
#    MainActivity kieruje je więc przez serwer lokalny Capacitora. Dzięki temu
#    zakładka Offline działa bez internetu tak jak w przeglądarce.
#
# 3. STAŁY KLUCZ PODPISU
#    Każdy świeży runner generował NOWY klucz debug, przez co nowego APK nie
#    dało się zainstalować na starym ("aplikacja nie została zainstalowana")
#    — trzeba było odinstalowywać. Klucz trzymamy w sekrecie
#    ANDROID_DEBUG_KEYSTORE (base64), więc kolejne buildy instalują się
#    normalnie jako aktualizacja. versionCode = numer builda.
#
# 4. BUILD NIE ODPALA SIĘ PRZY KAŻDYM COMMICIE
#    Tylko ręcznie albo przy zmianie plików, które faktycznie trafiają do APK.
#    Zmiany www/index.html nie wymagają już nowego APK.
#
# 5. WYDANIE NA GITHUBIE (tag android-v<numer>) ze stałym linkiem do pobrania.
# ══════════════════════════════════════════════════════════════════════════════

on:
  workflow_dispatch:
    inputs:
      android_auto:
        description: 'Wsparcie Android Auto (natywny odtwarzacz media3)'
        type: boolean
        default: true
  push:
    branches: [main]
    paths:
      - 'native/**'
      - 'package.json'
      - 'capacitor.config.json'
      - 'icon.png'
      - 'www/offline.html'
      - '.github/workflows/android.yml'

concurrency:
  group: android-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write     # potrzebne do utworzenia wydania

env:
  APK_BUILD: ${{ github.run_number }}
  SERVER_URL: https://muzyka.komodaoskara.pl

jobs:
  build-android:
    runs-on: ubuntu-latest
    steps:
      - name: Pobierz kod z repozytorium
        uses: actions/checkout@v4

      - name: Skonfiguruj Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Skonfiguruj JDK dla Androida
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Zainstaluj zależności npm
        run: |
          npm install --legacy-peer-deps
          npm install @capacitor/android@6 --legacy-peer-deps
          # Plugin MediaSession — MUSI być zainstalowany PRZED `cap add android`,
          # żeby projekt androidowy wciągnął go jako moduł natywny.
          npm install @jofr/capacitor-media-session@4 --legacy-peer-deps
          npm install -D @capacitor/assets --legacy-peer-deps

      - name: "Capacitor - ładowanie aplikacji z serwera"
        run: |
          python3 - <<'PYEOF'
          import json, os
          p = 'capacitor.config.json'
          c = json.load(open(p))
          srv = c.setdefault('server', {})
          srv['url'] = os.environ['SERVER_URL']
          # Strona pokazywana, gdy serwer jest nieosiągalny, a nie ma jeszcze
          # zapisanej kopii w service workerze (np. pierwsze uruchomienie bez sieci).
          if os.path.exists('www/offline.html'):
              srv['errorPath'] = 'offline.html'
          else:
              print('::warning::Brak www/offline.html - bez sieci bedzie standardowy blad WebView')
          # Po tym frontend rozpoznaje APK i jego numer builda — porównuje go
          # z najnowszym wydaniem i proponuje aktualizację.
          c['appendUserAgent'] = 'SoundWaveAPK/' + os.environ['APK_BUILD']
          json.dump(c, open(p, 'w'), indent=2)
          print(open(p).read())
          PYEOF

      - name: Usuń stary android/ i wygeneruj świeży
        run: |
          rm -rf android
          npx cap add android

      - name: Sprawdź czy plugin MediaSession został wpięty
        run: |
          echo "── Wpięte pluginy Capacitora ──"
          cat android/capacitor.settings.gradle
          if ! grep -q "capacitor-media-session" android/capacitor.settings.gradle; then
            echo "❌ BŁĄD: plugin @jofr/capacitor-media-session NIE został wpięty do projektu."
            echo "   Bez niego APK znowu nie będzie miał kontrolek na ekranie blokady."
            exit 1
          fi
          echo "✅ Plugin MediaSession wpięty poprawnie"

      - name: Instalacja Pillow
        run: python3 -m pip install Pillow

      - name: Przygotowanie ikon
        run: |
          python3 - <<EOF
          import os
          from PIL import Image
          icon_path = None
          for root, dirs, files in os.walk('.'):
              if any(p in root for p in ['node_modules','android','.git','.github','assets']): continue
              for f in files:
                  if f.lower() == 'icon.png': icon_path = os.path.join(root, f); break
              if icon_path: break
          if not icon_path: print("BŁĄD: brak icon.png"); exit(1)
          img = Image.open(icon_path)
          os.makedirs('assets', exist_ok=True)
          for n in ['icon-only','icon-foreground','icon-background','splash','splash-dark']:
              img.save(f'assets/{n}.png','PNG')
          img.save('icon.png','PNG'); img.save('splash.png','PNG')
          print(f"OK: {icon_path}")
          EOF

      - name: Wygeneruj ikony
        run: npx @capacitor/assets generate --android

      - name: "Podpis i numer wersji"
        env:
          KEYSTORE_B64: ${{ secrets.ANDROID_DEBUG_KEYSTORE }}
        run: |
          python3 - <<'PYEOF'
          import os, re, base64
          gp = 'android/app/build.gradle'
          g = open(gp).read()

          # ── versionCode = numer builda ──────────────────────────────────
          # Android odmawia instalacji "aktualizacji" o niższym versionCode,
          # a przy równym nie ma jak odróżnić wersji. Numer runu rośnie zawsze.
          build = os.environ['APK_BUILD']
          g, n1 = re.subn(r'versionCode\s+\d+', f'versionCode {build}', g, count=1)
          g, n2 = re.subn(r'versionName\s+"[^"]*"', f'versionName "1.{build}"', g, count=1)
          assert n1 and n2, 'Nie znaleziono versionCode/versionName w build.gradle'
          print(f'✓ versionCode {build}, versionName 1.{build}')

          # ── Stały klucz podpisu ─────────────────────────────────────────
          b64 = os.environ.get('KEYSTORE_B64', '').strip()
          if b64:
              ks = os.path.join(os.environ['RUNNER_TEMP'], 'soundwave-debug.keystore')
              with open(ks, 'wb') as f:
                  f.write(base64.b64decode(b64))
              # Nadpisujemy konfigurację wbudowanego podpisu "debug" —
              # buildType debug używa jej domyślnie, nic więcej nie trzeba.
              sign = ('android {\n'
                      '    signingConfigs {\n'
                      '        debug {\n'
                      f'            storeFile file("{ks}")\n'
                      '            storePassword "android"\n'
                      '            keyAlias "androiddebugkey"\n'
                      '            keyPassword "android"\n'
                      '        }\n'
                      '    }')
              g = g.replace('android {', sign, 1)
              print('✓ Podpis: stały klucz z sekretu ANDROID_DEBUG_KEYSTORE')
          else:
              print('::warning::Brak sekretu ANDROID_DEBUG_KEYSTORE - APK podpisany '
                    'losowym kluczem, NIE zainstaluje sie jako aktualizacja poprzedniego')

          open(gp, 'w').write(g)
          PYEOF

      - name: "Konfiguracja Android - uprawnienia + AudioFocus + WebView"
        run: |
          python3 - <<'PYEOF'
          import os, glob, re

          # ── 1. Uprawnienia ────────────────────────────────────────────────
          manifest = 'android/app/src/main/AndroidManifest.xml'
          with open(manifest) as f: m = f.read()

          needed = [
              'android.permission.FOREGROUND_SERVICE',
              # Wymagane od Androida 14 dla foreground service typu mediaPlayback
              # (plugin deklaruje foregroundServiceType="mediaPlayback").
              'android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK',
              'android.permission.WAKE_LOCK',
              'android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS',
              # KLUCZOWE od Androida 13: bez POST_NOTIFICATIONS powiadomienie
              # foreground service NIE jest pokazywane — usługa działa, ale
              # kontrolek na ekranie blokady nie widać.
              'android.permission.POST_NOTIFICATIONS',
          ]
          add = ''.join(
              f'\n    <uses-permission android:name="{p}"/>'
              for p in needed if p not in m
          )
          if add:
              m = m.replace('</manifest>', add + '\n</manifest>')
          with open(manifest, 'w') as f: f.write(m)
          print('✓ Uprawnienia OK')

          # ── 2. MainActivity.java ──────────────────────────────────────────
          java_files = glob.glob('android/app/src/main/java/**/*.java', recursive=True)
          main_activity = next((f for f in java_files if 'MainActivity' in f), None)
          assert main_activity, 'MainActivity.java nie znaleziony!'

          with open(main_activity) as f: j = f.read()

          # Capacitor generuje klase PUSTA: `public class MainActivity extends
          # BridgeActivity {}` - najpierw dopisujemy szkielet onCreate.
          if 'onCreate' not in j:
              j = re.sub(r'public class MainActivity extends BridgeActivity\s*\{\s*\}',
                         'public class MainActivity extends BridgeActivity {\n'
                         '    @Override\n'
                         '    public void onCreate(android.os.Bundle savedInstanceState) {\n'
                         '        super.onCreate(savedInstanceState);\n'
                         '    }\n'
                         '}', j)
              assert 'super.onCreate(savedInstanceState);' in j, \
                     'Nie udalo sie dodac onCreate do MainActivity'
              print('+ dodano szkielet onCreate (klasa byla pusta)')

          imports = (
              'import android.media.AudioAttributes;\n'
              'import android.media.AudioFocusRequest;\n'
              'import android.media.AudioManager;\n'
              'import android.webkit.WebSettings;\n'
          )
          j = j.replace('import com.getcapacitor.BridgeActivity;',
                        'import com.getcapacitor.BridgeActivity;\n' + imports)

          j = j.replace(
              'public class MainActivity extends BridgeActivity {',
              'public class MainActivity extends BridgeActivity {\n'
              '    private AudioFocusRequest mAudioFocusRequest;\n')

          setup = r'''
              // ── Service worker przez bridge Capacitora ──────────────────
              // Aplikacja ładuje stronę z serwera (server.url). Capacitor
              // wstrzykuje swój bridge do HTML-a w shouldInterceptRequest —
              // ale zapytania SERVICE WORKERA tamtędy nie przechodzą. Strona
              // podana przez SW nie miałaby więc window.Capacitor (zero
              // MediaSession i Android Auto). Kierujemy je przez ten sam
              // serwer lokalny: dokument z SW też dostaje bridge, a zapisana
              // w cache kopia działa potem offline razem z pluginami.
              try {
                  if (android.os.Build.VERSION.SDK_INT >= 24) {
                      android.webkit.ServiceWorkerController.getInstance().setServiceWorkerClient(
                          new android.webkit.ServiceWorkerClient() {
                              @Override
                              public android.webkit.WebResourceResponse shouldInterceptRequest(
                                      android.webkit.WebResourceRequest request) {
                                  try {
                                      return getBridge().getLocalServer().shouldInterceptRequest(request);
                                  } catch (Exception e) {
                                      return null;
                                  }
                              }
                          });
                  }
              } catch (Exception e) { e.printStackTrace(); }

              // ── WebView: audio bez gestu użytkownika ────────────────────
              try {
                  getBridge().getWebView().getSettings()
                      .setMediaPlaybackRequiresUserGesture(false);
                  getBridge().getWebView().getSettings()
                      .setMixedContentMode(WebSettings.MIXED_CONTENT_COMPATIBILITY_MODE);
              } catch (Exception e) { e.printStackTrace(); }

              // ── POST_NOTIFICATIONS (Android 13+) ────────────────────────
              try {
                  if (android.os.Build.VERSION.SDK_INT >= 33) {
                      if (checkSelfPermission("android.permission.POST_NOTIFICATIONS")
                              != android.content.pm.PackageManager.PERMISSION_GRANTED) {
                          requestPermissions(
                              new String[]{"android.permission.POST_NOTIFICATIONS"}, 8801);
                      }
                  }
              } catch (Exception e) { e.printStackTrace(); }

              // ── AudioFocus ──────────────────────────────────────────────
              try {
                  AudioManager am = (AudioManager) getSystemService(AUDIO_SERVICE);
                  mAudioFocusRequest = new AudioFocusRequest.Builder(
                          AudioManager.AUDIOFOCUS_GAIN)
                      .setAudioAttributes(new AudioAttributes.Builder()
                          .setUsage(AudioAttributes.USAGE_MEDIA)
                          .setContentType(AudioAttributes.CONTENT_TYPE_MUSIC)
                          .build())
                      .setWillPauseWhenDucked(false)
                      .setAcceptsDelayedFocusGain(true)
                      .setOnAudioFocusChangeListener(focusChange -> {
                          android.util.Log.d("SW", "AudioFocus change: " + focusChange);
                      })
                      .build();
                  int result = am.requestAudioFocus(mAudioFocusRequest);
                  android.util.Log.d("SW", "AudioFocus result: " + result);
              } catch (Exception e) {
                  android.util.Log.e("SW", "AudioFocus FAILED: " + e);
              }'''

          j = j.replace('super.onCreate(savedInstanceState);',
                        'super.onCreate(savedInstanceState);' + setup)

          destroy = '''
              @Override
              public void onDestroy() {
                  super.onDestroy();
                  try {
                      if (mAudioFocusRequest != null) {
                          AudioManager am = (AudioManager) getSystemService(AUDIO_SERVICE);
                          am.abandonAudioFocusRequest(mAudioFocusRequest);
                      }
                  } catch (Exception ignored) {}
              }'''
          last = j.rfind('}')
          j = j[:last] + destroy + '\n}'

          with open(main_activity, 'w') as f: f.write(j)
          print(f'✓ MainActivity zaktualizowany: {main_activity}')

          # ── 3. network_security_config.xml ──────────────────────────────
          os.makedirs('android/app/src/main/res/xml', exist_ok=True)
          with open('android/app/src/main/res/xml/network_security_config.xml','w') as f:
              f.write('<?xml version="1.0" encoding="utf-8"?>\n<network-security-config>\n'
                      '  <domain-config cleartextTrafficPermitted="false">\n'
                      '    <domain includeSubdomains="true">muzyka.komodaoskara.pl</domain>\n'
                      '  </domain-config>\n</network-security-config>')
          print('✓ Wszystko gotowe')
          PYEOF

      # Przy buildzie z pusha nie ma inputów (inputs.android_auto jest puste),
      # stąd warunek: Android Auto zawsze, chyba że ręcznie odznaczone.
      - name: "Android Auto - natywny odtwarzacz (media3)"
        if: ${{ github.event_name != 'workflow_dispatch' || inputs.android_auto }}
        run: |
          set -e
          PKG_DIR=$(dirname "$(find android/app/src/main/java -name MainActivity.java | head -1)")
          echo "Pakiet aplikacji: $PKG_DIR"

          # Zrodla natywne trzymamy jako PLIKI w repo (katalog native/).
          cp native/SoundwaveAuth.java         "$PKG_DIR/"
          cp native/SoundwaveApi.java          "$PKG_DIR/"
          cp native/SoundwaveMediaService.java "$PKG_DIR/"
          mkdir -p android/app/src/main/res/xml
          cp native/automotive_app_desc.xml android/app/src/main/res/xml/

          cat > /tmp/auto_patch.py <<'AUTOPATCH'
          import glob

          # ---- build.gradle: media3 przypiete na 1.4.1 ----------------------
          # 1.5+ wymaga compileSdk 35, a Capacitor 6 ustawia 34.
          gp = "android/app/build.gradle"
          g = open(gp).read()
          if "media3-session" not in g:
              g = g.replace("dependencies {",
                            'dependencies {\n'
                            '    implementation "androidx.media3:media3-exoplayer:1.4.1"\n'
                            '    implementation "androidx.media3:media3-session:1.4.1"', 1)
          if "abortOnError false" not in g:
              g = g.replace("android {",
                            "android {\n    lint {\n        abortOnError false\n    }", 1)
          open(gp, "w").write(g)
          print("OK build.gradle")

          # ---- AndroidManifest: serwis + deklaracja dla Auto ----------------
          mp = "android/app/src/main/AndroidManifest.xml"
          m = open(mp).read()
          if "SoundwaveMediaService" not in m:
              svc = (
                  '\n        <service\n'
                  '            android:name=".SoundwaveMediaService"\n'
                  '            android:exported="true"\n'
                  '            android:foregroundServiceType="mediaPlayback">\n'
                  '            <intent-filter>\n'
                  '                <action android:name="androidx.media3.session.MediaLibraryService" />\n'
                  '                <action android:name="android.media.browse.MediaBrowserService" />\n'
                  '            </intent-filter>\n'
                  '        </service>\n\n'
                  '        <meta-data\n'
                  '            android:name="com.google.android.gms.car.application"\n'
                  '            android:resource="@xml/automotive_app_desc" />\n'
              )
              m = m.replace("</application>", svc + "    </application>", 1)
              open(mp, "w").write(m)
          print("OK AndroidManifest")

          # ---- MainActivity: podanie WebView i danych logowania -------------
          ma = [f for f in glob.glob("android/app/src/main/java/**/*.java", recursive=True)
                if "MainActivity" in f][0]
          j = open(ma).read()
          assert "super.onCreate(savedInstanceState);" in j, \
                 "MainActivity nie ma onCreate - krok konfiguracji nie wykonal sie poprawnie"
          if "SoundwaveMediaService.attachWebView" not in j:
              j = j.replace("super.onCreate(savedInstanceState);",
                  "super.onCreate(savedInstanceState);\n"
                  "\n"
                  "        // Android Auto: natywny odtwarzacz musi umiec wyciszyc WebView,\n"
                  "        // zeby dwa odtwarzacze nie graly jednoczesnie.\n"
                  "        try {\n"
                  "            SoundwaveMediaService.attachWebView(getBridge().getWebView());\n"
                  "        } catch (Exception e) { e.printStackTrace(); }", 1)
              extra = (
                  "\n"
                  "    @Override\n"
                  "    public void onResume() {\n"
                  "        super.onResume();\n"
                  "        try {\n"
                  "            final android.webkit.WebView wv = getBridge().getWebView();\n"
                  "            SoundwaveAuth.syncFromWebView(this, wv);\n"
                  "            // Przy zimnym starcie strona bywa jeszcze niezaladowana i nie ma\n"
                  "            // tokenu w localStorage - druga proba po chwili to domyka.\n"
                  "            wv.postDelayed(new Runnable() {\n"
                  "                @Override public void run() {\n"
                  "                    SoundwaveAuth.syncFromWebView(MainActivity.this, wv);\n"
                  "                }\n"
                  "            }, 5000);\n"
                  "        } catch (Exception e) { e.printStackTrace(); }\n"
                  "    }\n"
              )
              last = j.rfind("}")
              j = j[:last] + extra + "}\n"
              open(ma, "w").write(j)
          print("OK MainActivity")
          AUTOPATCH
          python3 /tmp/auto_patch.py

      - name: "Android Auto - kontrola przed buildem"
        if: ${{ github.event_name != 'workflow_dispatch' || inputs.android_auto }}
        run: |
          PKG_DIR=$(dirname "$(find android/app/src/main/java -name MainActivity.java | head -1)")
          for f in SoundwaveAuth.java SoundwaveApi.java SoundwaveMediaService.java; do
            test -f "$PKG_DIR/$f" || { echo "BRAK $f"; exit 1; }
          done
          test -f android/app/src/main/res/xml/automotive_app_desc.xml || { echo "BRAK automotive_app_desc.xml"; exit 1; }
          grep -q SoundwaveMediaService android/app/src/main/AndroidManifest.xml || { echo "BRAK serwisu w manifescie"; exit 1; }
          grep -q media3-session android/app/build.gradle || { echo "BRAK zaleznosci media3"; exit 1; }
          grep -q attachWebView "$PKG_DIR/MainActivity.java" || { echo "MainActivity nie zostal zalatany"; exit 1; }
          echo "Wszystkie elementy Android Auto na miejscu"

      - name: Zsynchronizuj projekt
        run: npx cap sync android

      - name: Zbuduj APK
        run: |
          cd android && chmod +x gradlew && ./gradlew assembleDebug
          cp app/build/outputs/apk/debug/app-debug.apk "../SoundWave-$APK_BUILD.apk"

      - name: Udostępnij APK
        uses: actions/upload-artifact@v4
        with:
          name: Soundwave-Android-APK
          path: SoundWave-${{ env.APK_BUILD }}.apk

      - name: Wydanie na GitHubie
        uses: softprops/action-gh-release@v3
        with:
          tag_name: android-v${{ env.APK_BUILD }}
          name: SoundWave Android (build ${{ env.APK_BUILD }})
          body: |
            Pobierz `SoundWave-${{ env.APK_BUILD }}.apk` i zainstaluj.

            Aplikacja ładuje interfejs z serwera, więc zwykłe aktualizacje
            SoundWave pojawiają się w niej same. Nowy APK jest potrzebny tylko
            przy zmianach natywnych — aplikacja sama o tym powiadomi.
          files: SoundWave-${{ env.APK_BUILD }}.apk
          # "Latest" zostaje dla wydania desktopowego; aplikacja szuka
          # najnowszego APK po prefiksie tagu android-v.
          make_latest: false
